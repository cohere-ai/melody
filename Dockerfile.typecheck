FROM python:3.12-slim

WORKDIR /app

ARG VLLM_VERSION=0.15.0

RUN pip install uv

# Install dependencies including vllm
RUN uv pip install --system ty vllm==${VLLM_VERSION}

# Mount point for the repo
VOLUME /app

CMD ["ty", "check", "cohere_melody_vllm"]
